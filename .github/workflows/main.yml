on:
  push:
    branches:
      - master


jobs:
  build:
    runs-on: ubuntu-latest


    steps:
      - name: 'printing'
        run: ls
    
      - uses: actions/checkout@v3

      - name: 'printinglf;sdlmgsf;lgd,'
        run: ls
  